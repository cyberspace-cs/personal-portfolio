# 个人主页 · 本地构建环境说明

适用于 Windows 本地开发、构建与部署。站点为 **Vite + React + TypeScript + Tailwind** 作品集，
学习资料（happy-llm / hello-agents）以 **git 子模块** 形式托管在 `public/learning/`。

---

## 1. 环境要求

| 组件 | 版本 | 说明 |
|---|---|---|
| Node.js | **18+（已用 nvm Node 24.15.0 实测 `npm run build` 通过）** | Vite 6 要求 Node 18+，推荐 20 / 22 LTS 或 24 |
| npm | 随 Node 附带（≥ 9） | 也可用 pnpm / yarn |
| Git | ≥ 2.25 | 需支持 `git submodule` 与 `--depth` |

- Node 安装：官网 https://nodejs.org 下载 LTS，或用 nvm（推荐，便于切版本）：
  ```powershell
  # Windows 安装 nvm：https://github.com/coreybutler/nvm-windows
  nvm install 24
  nvm use 24
  node -v      # 期望 v24.x
  ```
  > 本项目已在本机（nvm Node 24.15.0）跑通 `npm install` + `npm run build`，无 TS 错误、无 CSS 警告（字体 `@import` 已移至 `index.css` 顶部）。
- 验证：
  ```powershell
  node -v      # 期望 v18 / v20 / v24 均可
  npm -v
  git --version
  ```

---

## 2. 拉取代码

```powershell
git clone https://gitee.com/buleboy8065/personal-portfolio.git
cd personal-portfolio
```

> 仓库同时存在于 GitHub（`cyberspace-cs/personal-portfolio`）。Gitee 与 GitHub 内容同源。

---

## 3. 安装依赖

```powershell
npm install
```

国内网络慢可切换镜像：

```powershell
npm config set registry https://registry.npmmirror.com
npm install
```

---

## 4. 初始化学习资料子模块（教程正文）

学习资料作为 git 子模块托管在 `public/learning/`：

- `public/learning/happy-llm` —— Datawhale《从零开始构建大模型》
- `public/learning/hello-agents` —— Datawhale《从零开始构建智能体》

### 4.1 拉取（浅克隆，推荐）

```powershell
git submodule update --init --recursive --depth 1
```

> `--depth 1` 只拉最新一版，可大幅加速 `hello-agents`（约 230MB）的拉取。

### 4.2 首次登记子模块（若尚未登记）

```powershell
git submodule add --depth 1 https://github.com/datawhalechina/happy-llm.git public/learning/happy-llm
git submodule add --depth 1 https://github.com/datawhalechina/hello-agents.git public/learning/hello-agents
git commit -m "chore: 登记学习资料子模块"
```

### 4.3 子模块内容如何被站点引用

Vite 会把 `public/` 目录内容**原样拷贝**到构建产物 `dist/`，因此子模块拉取后，
可通过站点相对路径直接访问教程文件，例如：

- `https://<你的域名>/learning/happy-llm/README.md`
- `https://<你的域名>/learning/hello-agents/README.md`

站点「学习资料」板块的 **阅读** 按钮即指向这些本地路径；**代码** 按钮指向 GitHub 源码仓库。

> 如需把 Markdown 渲染成在线阅读页（而非下载 `.md`），可后续在 `public/learning/`
> 下接入 docsify / VitePress（属于进阶项，不影响当前部署）。

---

## 5. 本地开发预览

```powershell
npm run dev
```

打开 http://localhost:5173 ，修改源码热更新。

---

## 6. 构建与本地预览产物

```powershell
npm run build      # 产物输出到 dist/
npm run preview    # 本地预览 dist/ 构建结果（默认 http://localhost:4173）
```

构建前请确保已执行第 4 节子模块初始化，否则 `public/learning/` 为空，
「学习资料」卡片的阅读链接会 404（卡片本身仍正常显示）。

---

## 7. 部署（Vercel）

- 仓库已连接 Vercel，**推送到 `main` 即自动构建部署**。
- 「学习资料」板块代码在 `learning` 分支；**上线前需把 `learning` 合并进 `main`**。

### 7.1 完整 Vercel 部署配置

新建 / 导入 GitHub 仓库 `cyberspace-cs/personal-portfolio` 后，按下表设置：

| 配置项 | 值 |
|---|---|
| Framework Preset | `Vite` |
| Root Directory | 仓库根即项目目录，留空或填 `.` |
| **Build Command** | `git submodule update --init --recursive --depth 1 && npm run build` |
| Output Directory | `dist` |
| Install Command | `npm install`（默认即可） |
| Node Version | `20.x`（或 22 / 24） |
| 部署分支 | `main` |

> **关键**：Build Command **必须包含 `git submodule update --init --recursive --depth 1`**，
> 否则 Vercel 不会拉取 `happy-llm` / `hello-agents`，线上 `/learning/*` 阅读链接会 404。
> 两个子模块均为 GitHub 公开仓库，Vercel 可直接拉取，无需额外授权。
>
> 推送到 `main` 即触发自动构建部署（Hobby 版构建超时 25 分钟，`--depth 1` 浅克隆可大幅加速 `hello-agents` 约 230MB 的拉取）。

### 7.2 注意构建体积与超时

- `hello-agents` 约 230MB，Hobby 版构建超时 25 分钟，**务必使用 `--depth 1` 浅克隆**。
- 若 `dist/` 体积过大导致部署缓慢，可在 `vercel.json` 或 `.vercelignore`
  中忽略子模块里的图片/视频等非必要资源。

---

## 8. 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| `public/learning/` 为空、阅读链接 404 | 未执行 `git submodule update --init`；或网络拉取失败，重试 |
| `hello-agents` 拉取慢 / 失败 | 网络抖动；重试或换网络，始终加 `--depth 1` |
| 推 `main` 后线上没出现学习资料 | 板块在 `learning` 分支，需先合并进 `main` |
| `npm run build` 报 TS 错误 | 执行 `npm run lint` 看详情；多为字段拼写不一致 |
| 本地能跑、Vercel 构建失败 | 多为子模块未拉取；按 7.1 处理 |

---

## 9. 分支约定

| 分支 | 用途 |
|---|---|
| `main` | 线上生产分支（Vercel 部署源），`API_BASE` 等保持生产配置 |
| `learning` | 学习资料板块功能分支，验证后合并进 `main` |
| `prod-api` | 刷题教练 H5/小程序指向生产域名 `https://taoxie.vip` |
| `master`（本地旧历史） | 早期本地调试，保留 `localhost` 配置，与远程不同步 |

> 工作流：在功能分支开发 → 本地 `npm run build` 验证 → 合并进 `main` → 推送触发 Vercel 部署。
