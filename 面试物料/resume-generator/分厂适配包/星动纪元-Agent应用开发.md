# 星动纪元 · 具身 Agent 应用开发工程师 —— 面试适配包

> 投递前：使用对应分厂 docx 简历（`output/星动纪元-Agent应用开发.docx`）并以官网最新 JD 复核；本文件含「招聘网址 + 简历匹配 + 面试题库 + 话术」。

## 1. 官方招聘网址
- 官网 / 招聘入口：**https://www.robotera.com/**
- Boss 直聘可搜索「星动纪元 具身 Agent 应用开发工程师」同岗位（详情页有反爬，以官网为准）。

## 2. 简历匹配要点（投递前核对）
- **求职意向**：具身 Agent 应用开发工程师
- **定向总结**：面向星动纪元的具身智能 Agent 方向：
- **重点强调**：星动纪元：具身智能创业公司
- **岗位匹配关键词**：具身 Agent / 端侧部署 / 任务规划 / 模仿学习 / 工具调用
- **项目 Phase 重排**：D → F → A → G → C → B → E → H（其余按默认序）
- **对应简历文件**：`output/星动纪元-Agent应用开发.docx`

## 3. 面试题库（按本岗侧重挑选）
> 来源：2026 公开经验帖（字节 Agent 四面 21 题、小林 coding Agent16/RAG20/工具调用16/工程22、具身 Gank 面经、DeepSeek 模型专项）。完整总库见 `面试题库总库.md`。

### 具身智能专项（8 题）
1. **Q**：大模型装进机器人与纯软件 Agent 有何不同？
   **A**：感知需3D视觉+Affordance；控制输出 Action Token 且面临低频推理(1-5Hz)vs高频控制(>500Hz)；幻觉会撞人需硬件级安全；数据饥渴需仿真飞轮。
2. **Q**：分层(Pipeline) vs 端到端(VLA/RT-2)架构区别？
   **A**：Pipeline 感知-规划-控制分离、可解释可控；VLA 图像+语言直接映射 Action Token，端到端泛化但难调试。
3. **Q**：如何解决真实数据稀缺？
   **A**：Sim-to-Real(域随机化/系统辨识)+模仿学习预热+强化学习微调+合成数据填补物理荒漠。
4. **Q**：RT-1 与 RT-2 核心区别？为何 RT-2 是 VLA？
   **A**：RT-1 输出离散动作；RT-2 把动作也 Token 化进同一个 VL 模型自回归预测，打通文本与动作。
5. **Q**：Affordance(可供性)是什么？
   **A**：物体能被如何操作的知识(如海绵是软的、可抓取)；LLM 语义需对齐重力/臂展等物理常识防幻觉动作。
6. **Q**：频率失配(1Hz大脑 vs 500Hz身体)如何解决？
   **A**：分层混合：慢思考(VLA/LLM)做高层决策 + 快执行(MPC/PID)做底层控制，解耦。
7. **Q**：如何防止大模型幻觉伤人？
   **A**：安全过滤器+动力学校验+看门狗机制+多模态一致性校验，硬件级兜底。
8. **Q**：如何评估具身模型好坏？只看 PPL 够吗？
   **A**：不够；看任务成功率(Success Rate)/完成度/泛化，结合仿真与真机指标。

### Agent 系统设计（14 题）
1. **Q**：什么是 Agent？与大模型本质不同？
   **A**：Agent = 思维链 + Function Calling + 循环；模型自主决定下一步，而非逐步指挥（概率性自主运行）。
2. **Q**：Agent 核心组件？
   **A**：规划(Planning) + 记忆(Memory) + 工具(Tools) + 执行(Execution) + 反思(Reflection)。
3. **Q**：Workflow / Agent / Tools 区别？
   **A**：Tools 是原子能力；Workflow 是固定编排(确定性)；Agent 是模型动态决策(概率性)。
4. **Q**：ReAct 是什么、如何实现？
   **A**：Reason+Act 交替：先推理再调工具观察结果，循环至完成；实现上每轮输出 Thought/Action/Observation。
5. **Q**：ReAct / Plan-and-Execute / Reflection 区别与选型？
   **A**：ReAct 边想边做(简单)；Plan-and-Execute 先整体规划再执行(复杂长任务)；Reflection 加自评纠偏(质量要求高)。
6. **Q**：复杂任务如何拆分？为什么要拆分？
   **A**：拆成明确输入输出的子任务，便于校验/并行/回滚；单任务失败不影响整体。
7. **Q**：记忆机制如何设计(短期/长期)？
   **A**：短期=最近N轮+工作记忆(关键约束)；长期=向量库/SQLite 外部存储按需检索；分层+衰减。
8. **Q**：记忆压缩有哪些方法？
   **A**：滑动窗口、对话摘要、关键信息提取(决策/约束/偏好每轮注入)、向量库检索长期。
9. **Q**：多 Agent 如何协作与动态切换？
   **A**：Supervisor 拆任务→Worker 并行→Synthesizer 汇总；消息队列解耦；按任务复杂度动态选子 Agent。
10. **Q**：为什么有时手搓 Agent 而非用框架？
   **A**：框架有抽象开销/黑盒；手搓对循环、记忆、工具调用完全可控，便于调试与极致优化。
11. **Q**：如何赋予 LLM 规划能力？
   **A**：Prompt 引导分步 + 外部 Planner/ReAct + 任务树；复杂场景用 Plan-and-Execute。
12. **Q**：反思机制是什么、为什么、怎么实现？
   **A**：让模型/独立检查 Agent 复盘输出矛盾并回退重规划；防环防偏；节点校验+循环检测。
13. **Q**：如何解决上下文漂移？
   **A**：每轮注入原始目标 + 阶段性总结 + 上下文压缩 + 独立监督 Agent 拉回。
14. **Q**：如何防止工具调用幻觉？
   **A**：严格工具 Schema + 工具白名单(执行层拦截未注册) + 参数校验(类型/枚举)。

### 推理优化与部署（6 题）
1. **Q**：如何降低推理成本？
   **A**：模型选择(小模型做简任务) + KV Cache + 量化(INT8/INT4) + 批处理(continuous batching)。
2. **Q**：KV Cache 是什么、为什么重要？
   **A**：缓存历史 Token 的 K/V 避免重复计算，自回归生成最基础也最明显的加速。
3. **Q**：量化怎么做、精度损失？
   **A**：FP16→INT8/INT4，精度损失有限但速度与显存大幅下降；AWQ/GPTQ 保关键权重。
4. **Q**：vLLM / continuous batching 是什么？
   **A**：vLLM 用 PagedAttention 管理 KV；continuous batching 动态拼请求提升 GPU 利用率。
5. **Q**：混合路由与限流器为什么重要？
   **A**：简单任务走小模型省成本、复杂走大模型保质量；令牌桶限流防 429，关键路径优先。
6. **Q**：端侧部署怎么做？
   **A**：量化+蒸馏压缩模型，配合推理框架(如 ONNX/TFLite)在移动/边缘跑，低延迟。

### 工程落地与系统能力（5 题）
1. **Q**：Agent 执行链路如何设计、保证连续任务正确？
   **A**：输入→规划→拆子任务→执行→汇总→判完成；状态持久化+Checkpoint+结果校验+超时重试+人工介入点。
2. **Q**：长任务如何保证不偏离目标？
   **A**：每轮注入原始目标 + 阶段自查 + 外部监督 Agent + 任务分解 + 上下文压缩。
3. **Q**：工具调用安全如何做(Key泄露/敏感信息/Prompt注入)？
   **A**：Key 不进上下文(执行层注入)+最小权限+输出过滤+Prompt注入检测+审计日志。
4. **Q**：传统 Web 与 AI Agent 应用区别？
   **A**：Web 确定性/可单测；Agent 概率性/循环态/成本按 Token/靠评估与 Bad Case。
5. **Q**：如何评估生成模型多样性与准确性？
   **A**：多样性 CLIP Score 方差/FID；准确性 CLIP Score+人工+A/B；Bad Case 分析闭环。

## 4. 话术（STAR 模板 + 本项目定制）
> 以下话术基于本项目「刷题教练 → 定制化备考 Agent v3.1.0-agent-mem」设计，请按你的真实经历微调数据与措辞。

### 多Agent
- **面试官可能问**：多 Agent 怎么设计？为什么不用单一 Agent？
- **话术（STAR）**：S：单 Agent 在长任务里易上下文漂移、难并行。 T：需要角色分工与防环。 A：Supervisor 拆子任务→Worker 并行(陪练/诊断/规划)→Synthesizer 汇总；反思节点校验矛盾并回退重规划；循环检测+拒绝兜底防死循环；同比 AutoGen/CrewAI 同构。 R：复杂备考任务一致性提升，用户纠错成本下降。

### 推理优化
- **面试官可能问**：推理优化你做过什么？
- **话术（STAR）**：S：高质量陪练成本高，免费用户难用。 T：在保质量前提下降本提速。 A：vLLM/continuous batching + KV Cache 复用 + INT8 量化 + 投机解码；Benchmark 化推理链路。 R：speedup≈8x、kv_cache_hit≈0.8，免费用户也能用高质量陪练，扩大漏斗顶部。

### 工程落地
- **面试官可能问**：Agent 系统如何保证连续任务正确与可降级？
- **话术（STAR）**：S：无 API Key 或工具失败会导致系统不可用。 T：保证高可用与正确性。 A：状态持久化+Checkpoint；工具白名单+参数校验防调用幻觉；无 Key 时纯检索+RAG 规则降级；可观测采集全链路。 R：规则降级可用性 100%，关键路径不卡死。

### 职业规划
- **面试官可能问**：为什么选 Agent 应用开发 / 为什么投我们？
- **话术（STAR）**：S：看到 2026 招聘从「大模型基础」转向「AI 应用落地」，Agent 是核心。 T：希望在最落地、最需要工程深度的团队打磨。 A：已具备 Agent 框架/RAG 防幻觉/多 Agent/推理优化/评测闭环全链路经验，且持续跟进 MCP/Skill 等 2026 新标配。 R：希望把可量化、可降级的 Agent 落地方法论带到贵司 {岗位}。

## 5. 反问清单（面试官问「你有什么想问的」）
- 该岗位负责的 Agent 业务当前最痛的点是「能力」还是「落地/成本」？团队更看重哪块？
- Agent 评测体系现状：是离线四率为主，还是已有在线 A/B / 可观测看板？
- 技术栈是否会走向 MCP / Skill 标准化？工具接入是框架封装还是自研 Harness？
- 当前推理成本与并发压力如何？是否有端侧/量化部署需求？
- 这个岗位 0→1 还是 1→N？OKR 更偏业务指标还是技术基建？
- 团队协作上，算法/工程/产品如何分工？产品经理是否参与 Agent 评测指标定义？

## 6. 投递前 CheckList
- [ ] 用 `output/星动纪元-Agent应用开发.docx` 而非通用简历
- [ ] 以 https://www.robotera.com/ 最新 JD 复核「求职意向 / 关键词 / Phase 重排」
- [ ] 本适配包第 3 节题库过一遍，第 4 节话术能脱稿讲
- [ ] 准备 1-2 个反问（第 5 节）
