===== PAGE 1/167 =====

Agent项目
01-代码仓库上下文设计
02-运行时主循环与执行编排设计
05-结构化的会话记忆
06-上下文瘦身与输出管理设计
0417字节Agent开发一面

===== PAGE 2/167 =====

Pico：本地代码智能体 Harness
核心技术：Python、Agent Harness、Tool Calling、Context Management、Checkpoint / Resume、
Layered Memory、Run Trace
面向代码仓库长链路任务开发本地代码 agent harness，围绕模型接入、工具调用、上下文管理、任务
恢复、结构化记忆、运行审计和评测闭环做系统化设计，重点解决多轮任务里 prompt 膨胀、重复读文
1. Agent Harness 架构设计：负责本地代码 agent 的整体设计与开发，统一模型接入、工具执行、会
2. 长上下文治理：设计分层上下文管理与预算裁剪机制，在 12 组长上下文配置里，将平均 prompt 长
3. 结构化记忆系统：针对多轮任务里 agent 反复读同一文件、重复确认已知事实的问题，把任务摘
要、文件摘要、过程笔记和相关记忆召回做了分层；在 12 个记忆依赖任务里，follow-up 阶段的重
复读文件次数从 60 次降到 0 次，且不再需要额外工具调用去重新确认已经拿到的事实。
4. 任务恢复机制：设计 checkpoint / resume 机制，让 agent 在上下文超预算、中断恢复和 
workspace 漂移识别率 100%，且没有出现误信旧状态继续执行的情况。
5. 工具安全与运行治理：构建标准化工具调用与安全边界，覆盖参数校验、工作区隔离、高风险审
批、重复调用拦截、敏感信息脱敏和 partial success 识别；在固定回归任务中保持 100% 通过
6. 评测与审计闭环：：将评测拆成 harness regression、上下文治理、记忆收益和恢复正确性几层，
version 2.0 优化记忆模块，支撑面试深度讲

===== PAGE 3/167 =====

Pico：本地代码智能体Harness
Python、Agent Harness、Tool Calling、Context Engineering、Memory Retrieval、Prompt Cache
面向代码仓库长链路任务开发本地代码 Agent Harness，覆盖模型接入、工具调用、上下文管理、结构
化记忆、运行审计与评测闭环，重点解决多轮任务中的上下文膨胀、重复读取、工具误调用和结果不可
1. Agent Harness 架构设计：负责本地代码 Agent 的整体设计与开发，统一模型接入、工具执行、会
2. 长上下文治理：针对代码仓库长链路任务中的 prompt 膨胀问题，设计分层上下文管理与预算裁剪
机制；在 12 组真实长上下文配置上，将平均 prompt 长度从 6964 压缩到 5418，平均压缩率 
3. 结构化记忆系统：将任务摘要、文件摘要和会话笔记分层管理，并结合 freshness 校验和相关记忆
召回减少重复读取；在 12 个真实记忆依赖任务中，将重复读文件次数从 8 次降到 3 次，平均工具
4. 工具安全与运行治理：构建标准化工具调用与安全边界，覆盖参数校验、工作区隔离、高风险审
自动化测试。

===== PAGE 5/167 =====

📎01-代码仓库上下文设计.md📎02-运行时主循环与执行编排设计.md📎03-工具的接入与调用设计.md
📎04-提示词形态与缓存复用设计.md📎05-结构化的会话记忆.md
📎06-上下文瘦身与输出管理设计.md📎07-会话状态、运行工件与恢复机制设计.md

===== PAGE 6/167 =====

a. 比如：这个项目到底解决了什么问题？为什么叫 harness，不是普通 agent？上下文、记忆、
用户请求进来之后，CLI 怎么组装 agent，runtime 怎么进入主循环，context 怎么拼 prompt，模
型怎么返回结果，工具怎么执行，记忆怎么更新，最后trace之类的怎么落盘。

===== PAGE 7/167 =====

改一下上下文裁剪策略；
补一个记忆召回规则；

===== PAGE 9/167 =====

缓存保留策略默认是 ﻿in_memory﻿

===== PAGE 13/167 =====

在很多真实的落地场景里，模型外围的配套系统——比如工具调用、上下文管理和记忆功能——发
这个项目最核心的地方就是通过编写一个轻量化的code agent来展示你在面试时候对agent和
那接下来就让我补充一点前置知识，来一点点回答：如何编写一个code agent让LLM发挥更大的威
最近（2026/04）的后端/agent面试，逃不开的话题就是claude code和codex，很多面试官会考
2. 有没有了解里面的一下架构设计，甚至把一些思想落地到自己的agent开发中
像claude code和codex，它们本质上都是agent化的编程工具。它们把大语言模型包裹在一个应
用层（也就是我们说的 Agent harness）里，从而在处理编程任务时更加顺手，表现也更好。
code agent是专门为软件开发打造的，在这之中，重要的可不仅仅是你选了哪个模型，更在于外
围的配套系统——包括代码仓库的上下文、工具的设计、提示词缓存的稳定性、记忆能力，以及能
我们的目标：如何编写一个code agent让LLM发挥更大的威力

===== PAGE 14/167 =====

型，体验一下同一模型在不同code agent下的任务表现，应该就能发现，其实模型强是我们需要
的，但一个好的code agent设计更是我们当前所急需的）
弄清楚这个区别很重要。因为当我们聊起大语言模型（LLM）时，大家往往会把模型本身、模型
的推理行为和智能体产品混为一谈。
所以在探讨code agent到细节之前，我们先花点时间，简单梳理一下大语言模型、推理模型和智
大语言模型（LLM） 是核心，本质上它就是一个不断预测“下一个词”的模型。
推理模型（Reasoning Model） 其实也是 LLM，只不过它经过了特殊的训练或提示词引导，会在
生成答案时投入更多的算力（即增加推理时间的算力消耗，test-time compute），用来做中间步
骤的推理、自我验证，或者在多个候选答案中搜索最佳结果。
智能体（Agent） 则是盖在模型上面的一层，你可以把它理解为一个围绕模型运转的control 
通常情况是这样的：你给出一个目标，Agent层（或者说 Harness）就会替模型做决定：接下来要
LLM 像一个普通但合格的厨师，日常炒几个家常菜没问题，出菜也快。
推理模型像一个更强的大厨，做复杂菜、宴席菜、更讲究火候和步骤的菜会更稳，但也更耗时
Agent harness 则不是厨师本身，而是整个后厨的调度系统：点单、备菜、分工、计时、查菜
你当然可以让一个厨师单独做菜，就像直接调用 LLM 或推理模型。但如果要稳定地做一整桌菜，
而是要看后厨系统是不是成熟。所以，LLM 和推理模型决定谁来做菜、做菜能力有多强；Agent 
换句话说，Agent就是一个在特定环境里不断循环调用模型的系统。
大语言模型 (LLM): 最原始的基础模型。
推理模型 (Reasoning model): 优化过的 LLM，专门为了输出中间推理过程（也就是我们常说
大语言模型、推理模型与智能体

===== PAGE 15/167 =====

智能体 (Agent): 一个包含了“模型 + 工具 + 记忆 + 环境反馈”的循环系统。
Agent harness (智能体运行框架): 围绕智能体搭建的软件脚手架，负责管理上下文、工具调
Coding harness (编程运行框架): Agent harness 的“特化版”，专门针对软件工程量身定制，
负责管理代码上下文、开发工具、代码执行和迭代反馈。
就像上面列出的，在讨论智能体和编程工具时，我们常会碰到这两个词：Agent harness（智能体
代码的软件脚手架。而 Agent harness 的适用面更广，不仅限于编程（比如 OpenClaw）。
总而言之：更好的 LLM 能为推理模型打下更坚实的基础（当然还需要额外的训练），而优秀的 
Harness 则能把推理模型的潜力压榨到极致。
当然，LLM 和推理模型光靠自己（不用任何 Harness）也能解决一些编程问题。
但是，真实的写代码可不仅仅是大模型最擅长的预测下一个词。
看到这里，你可以开始看后面的01-08的章节，一步步思考，怎么设计一个好的code agent

===== PAGE 16/167 =====

memory﻿、﻿checkpoint﻿、﻿resume﻿、﻿.pico/﻿ 这些状态到底各干什么
上下文不是乱塞进 prompt，是按 section 和预算组织
记忆、checkpoint、恢复和运行工件会一起把状态留住

===== PAGE 17/167 =====

上 ﻿上下文管理﻿、﻿模型适配层﻿ 和 ﻿工具边界 / 护栏﻿。这个部分回答的是，一条请求进来以后，系
第二条是状态面。它把 ﻿记忆 / 恢复﻿ 和 ﻿持久化﻿ 放在一起。这里最关键是这些状态各自解决不同问
题。session 让系统还能继续干，run artifacts 让你知道刚才到底发生了什么，durable memory 则负责
memory.py﻿、﻿run_store.py﻿、﻿task_state.py﻿

===== PAGE 18/167 =====

build_agent()﻿ 把 ﻿WorkspaceContext﻿、﻿SessionStore﻿ 和具体 ﻿ModelClient﻿ 装
5. 每一轮都会重新构建 prompt，把 prefix、memory、history 和 current request 拼起来。
6. 模型返回结果以后，runtime 先 ﻿parse()﻿，判断这是工具调用、最终答案，还是一次需要重试的
7. 如果是工具调用，就走 ﻿run_tool()﻿，在统一边界里校验、审批、执行、比对工作区变化，再把
8. 这轮发生过的事情会进入 session history、working memory、trace、checkpoint，必要时还会写 
durable memory。

===== PAGE 20/167 =====

def build_agent(args):

===== PAGE 21/167 =====

这哥agent loop值得大家反复去可能啊：
第一，prompt﻿ 不是在请求开始时构建一次就算了，而是每轮都重建。因为工作区、memory、
五、上下文管理：不是把信息都塞进 prompt

===== PAGE 22/167 =====

很多 agent 一开始都栽在同一个地方，前几轮还好，跑到后面 prompt 越来越胖，历史
memory﻿
relevant memory﻿
关记忆，还是稳定前缀，这三件事的后果完全不一样。把这些 section 拆开以后，prompt 组装这件事就
relevant_memory -> history -> memory -> prefix

===== PAGE 23/167 =====

对 code agent 来说，危险的地方在于调用工具之前有没有统一边界。

===== PAGE 24/167 =====

memory、trace、benchmark 都会变得不可信，因为你已经不知道系统到底做过什么。
七、记忆、Checkpoint 与恢复：系统怎么持续工作

===== PAGE 25/167 =====

pico﻿ 现在的记忆不是单层结构，而是至少分成三部分：
working memory，也就是 ﻿LayeredMemory﻿
durable memory，也就是 ﻿DurableMemoryStore﻿
LayeredMemory﻿ 负责当前工作面，里面主要是这些东西：
DurableMemoryStore﻿ 负责长期仍然成立的事实。它不再继续塞在 session JSON 里，而是单独落
到 ﻿.pico/memory/﻿。这部分不是所有输出都自动写进去，而是要经过 promotion gate。比如项目约

===== PAGE 27/167 =====

pico﻿ 到这里已经不只是一个能调工具的 agent 了。它还得回答这些问题：

===== PAGE 28/167 =====

└── memory/
    ├── MEMORY.md

===== PAGE 29/167 =====

memory/﻿ 存的是 durable memory，它面向的是跨会话仍然成立的长期事实。
pico/memory.py﻿
记忆内核
working memory、durable 
memory、retrieval 都在这里

===== PAGE 30/167 =====

pico/memory.py﻿
3. 然后看上下文管理图和工具边界图，记住 prompt 和工具都不是黑盒。
4. 再看记忆 / 恢复图，确认 working memory、durable memory、checkpoint 的分工。

===== PAGE 31/167 =====

01-代码仓库上下文设计
这部分内容解决一个问题：code agent 进到代码仓库以后，首先应该拿到什么上下文。
在使用code agent到时候用户经常只会给一句很短的话，比如修一下测试、加个接口、看下这里为什么
对人来说，这种话还能靠经验脑补上下文。
所以仓库上下文这一层要做的事很明确，先回答这几个最影响首步判断的问题：

===== PAGE 32/167 =====

入口在 ﻿pico/cli.py﻿。﻿build_agent()﻿ 会先跑一次 ﻿WorkspaceContext.build(args.cw
把高价值项目文档先带进上下文

===== PAGE 33/167 =====

CLI build_agent()
DOC_NAMES = ("AGENTS.md", "README.md", "pyproject.toml", "package.json")

===== PAGE 34/167 =====

AGENTS.md﻿ 通常会规定工作方式、测试方式、提交约束
agent 的行为规则

===== PAGE 35/167 =====

对面试来说，这里有个很好讲的点。很多人会把 prompt 理解成一整块文本，但在 agent runtime 里，
内容塞进去，花掉的是上下文预算，换来的不一定是更高成功率。

===== PAGE 36/167 =====

准，反而可能因为上下文变长和噪声增加，让真正重要的线索被稀释。
所以这里的取舍很明确，仓库上下文这一层先做导航，不做知识库。
当前只看 ﻿AGENTS.md﻿、﻿README.md﻿、﻿pyproject.toml﻿、﻿package.json﻿。这对很多项目
这些边界不影响第一版成立，但它们会直接影响 agent 进复杂仓库时的第一步判断速度。

===== PAGE 37/167 =====

Pico﻿ 这层仓库上下文设计，核心是让模型先站在一个正确的位置上开始行动。
口最值得先看。后面的文件阅读、字符串搜索、测试执行和记忆回写，都是在这层基线之上继续往前
代码 agent 进仓库后的第一步，是先拿一份仓库基线。Pico 这里收的都是第一步最值钱的稳定事实，

===== PAGE 38/167 =====

02-运行时主循环与执行编排设计
如果只看 ﻿tools.py﻿、﻿memory.py﻿、﻿context_manager.py﻿ 这些模块，很容易把项目理解成
对面试来说，这一层很关键。因为很多人能讲清工具、能讲清 memory、也能讲清 prompt，但一问到一
一条用户请求，怎么被 runtime 推进成一次有边界、有状态、可回放的 agent 执行。
高风险工具怎么审批

===== PAGE 39/167 =====

这些东西定完以后，运行权就交给 ﻿agent.ask(user_message)﻿。

===== PAGE 40/167 =====

update history / task_state / trace / memory

===== PAGE 41/167 =====

pico/runtime.py﻿ 里的 ﻿ask()﻿，开头先立运行现场，再进入推理和执行。
    self.memory.set_task_summary(user_message)

===== PAGE 42/167 =====

tool_steps﻿ 统计真正进入执行阶段的工具调用次数

===== PAGE 43/167 =====

memory
当前请求对应的相关上下文
它支持两类工具调用格式：

===== PAGE 44/167 =====

4. 高风险工具走审批
7. 更新 memory 里的局部事实
memory 里的轻量事实
工具调用怎么重新接回主循环

===== PAGE 45/167 =====

这样 runtime 才能在下一轮继续推进，而不是每次工具调用都像一条临时岔路。

===== PAGE 46/167 =====

这也是为什么 ﻿ask()﻿ 看上去比普通 demo agent 重一点。它不是只求跑通，还要把每一步都留证
减少了花哨编排

===== PAGE 47/167 =====

工具调用、模型调用、状态回写都还在一个明确的总调度器里。
后面如果子 agent 变多，父子 run 的关系值得被单独建模。

===== PAGE 48/167 =====

面的工具治理、记忆系统、缓存和审计，最后都要挂在这条线上，整个 agent 才像一个真正能持续工作
memory，继续下一轮。停机条件也很清楚，要么拿到 final answer，要么命中 step limit 或 retry 

===== PAGE 49/167 =====

很多人一说 agent 会用工具，注意力就会跑到能力扩展上。能读文件、能搜字符串、能跑 shell、能改文
一条工具调用在执行前会被拦哪些东西

===== PAGE 50/167 =====

structured tool call
history / trace / memory

===== PAGE 51/167 =====

除此之外，还有一个条件注册的 ﻿delegate﻿。当前 agent 深度还没超过上限时，它才会出现在工具表
第二，delegate﻿ 这种子 agent 能力也没有默认放开，它本身也是受限工具。
if agent.depth < agent.max_depth:
e, agent)}

===== PAGE 52/167 =====

risky﻿ 决定它要不要走审批
4. 高风险工具走审批
6. 裁剪结果并写回 memory
未知工具、非法参数、重复调用、审批拒绝，这些情况都在真正执行之前就被挡住了。
一条工具调用在执行前会拦什么
    return "error: repeated identical tool call ..."
self.update_memory_after_tool(name, args, result)

===== PAGE 53/167 =====

这个约束确实更保守，但失败原因会很清楚。对 agent 来说，这一点很重要。runtime 能早点把错误变
    path = agent.path(args["path"])

===== PAGE 54/167 =====

它本质上是一个受限子任务。代码里对子 agent 的约束很明确：
    cwd=agent.root,
    env=agent.shell_env(),

===== PAGE 55/167 =====

对读写文件类工具，memory 会更新最近路径和局部摘要
这一点很关键，因为工具层执行完以后，结果还要继续服务下一轮推理和后面的审计。
    depth=agent.depth + 1,
    max_depth=agent.max_depth,
return f"error: repeated identical tool call for {name}; choose a differen

===== PAGE 56/167 =====

审批拒绝在 ﻿tests/test_safety_invariants.py﻿
delegate﻿ 深度限制和只读子 agent 在 ﻿tests/test_safety_invariants.py﻿、﻿tests/

===== PAGE 57/167 =====

PS：sandbox对于code agent还是很重要的，但作为一个实习/秋招项目，我没有扩展开增加大家的学
trace 和 memory。这样模型拿到工具以后，系统还能保持清楚的边界和可解释的行为。对一个本地、轻
() ，按顺序检查工具是否存在、参数是否合法、是不是重复调用、高风险动作要不要审批。路径默认

===== PAGE 58/167 =====

如果每一轮都把系统规则、工具说明、工作区摘要、记忆、历史和当前请求混在一起，整体功能当然能
写代码这类长链路任务有个很明显的特点。每一轮真的在变的，通常是最近历史、当前记忆、用户刚提
会话里刚积累出来的短期记忆

===== PAGE 60/167 =====

你可以把它理解成 agent 的工作手册。它会告诉模型：
    You are pico, a small local coding agent working inside a local reposi

===== PAGE 61/167 =====

README.md﻿、﻿AGENTS.md﻿ 这些白名单文档变化

===== PAGE 62/167 =====

memory 变化
memory﻿
relevant_memory﻿
prompt_cache_retention = "in_memory"﻿

===== PAGE 63/167 =====

    prompt_cache_retention = "in_memory"

===== PAGE 64/167 =====

prompt_cache_retention﻿ 还是固定写死为 ﻿"in_memory"﻿

===== PAGE 65/167 =====

现在固定成 ﻿"in_memory"﻿，先跑通链路没问题。后面如果任务更长、后端更多，这一层最好能根据

===== PAGE 66/167 =====

05-结构化的会话记忆
Pico﻿ 到了记忆这一层，真正要解决的不是“像人一样记住很多事”，而是一个更具体的问题，模型是
所以对于code agent的设计，不做大记忆系统，也不做会话知识库。
Pico﻿ 现在把哪些东西记进 memory
这套轻量记忆为什么真的减少了重复读取
这些问题回答不清楚的话，记忆层就很容易变成一个模糊概念，看起来高级，但说不清到底解决了什
Pico﻿ 记忆的目标很明确：
先让记忆保持可解释
Pico 的记忆不是知识库

===== PAGE 67/167 =====

它没有一上来走向量检索，也没有把所有历史都变成一个大召回池。
目前的设计更像一个轻量工作记忆，主要围绕当前任务、最近文件、文件摘要和几条跨轮笔记来组织。
记忆结构定义在 ﻿pico/memory.py﻿ 的 ﻿default_memory_state()﻿。
真正承担记忆职责的，主要是这四块：
记忆在代码里长什么样
self.memory.set_task_summary(user_message)

===== PAGE 68/167 =====

这一层是 ﻿Pico﻿ 记忆里最有用的一层。
read_file﻿ 的结果不会原样塞进 memory，而是先走 ﻿summarize_read_result()﻿，压成一个
这意味着记忆里留下的不是整段文件内容，而是一句足够提醒下一轮“刚才读到了什么”的短结论。
它不是 embedding 检索，规则很透明：
summary = memorylib.summarize_read_result(result)
self.memory.set_file_summary(canonical_path, summary)

===== PAGE 69/167 =====

这一层是 ﻿Pico﻿ 记忆设计里很关键的地方。
如果文件摘要不能感知文件是不是已经变了，记忆就会开始把旧结论当真。对代码 agent 来说，这比“没
等到 ﻿render_memory_text()﻿ 再把这些摘要渲染给模型看时，它会重新算一次当前文件的 
这样记忆层至少能避开一个很常见的问题，文件已经变了，模型还在拿旧摘要继续推理。
这部分设计在 ﻿pico/runtime.py﻿ 的 ﻿update_memory_after_tool()﻿。
新事件怎么同时进入历史和记忆
    self.memory.invalidate_file_summary(canonical_path)

===== PAGE 70/167 =====

2. 只有少量高价值结果会继续沉淀进 ﻿memory﻿
现在跟记忆绑定最紧的是这三类工具：
这个设计的意思很清楚，记忆不是凭空总结出来的，它跟工具链路绑在一起。
到了下一轮，ContextManager.build(user_message)﻿ 会把 memory 分成两部分送回去：
memory﻿
relevant_memory﻿
memory﻿ 由 ﻿render_memory_text()﻿ 渲染，主要展示：
relevant_memory﻿ 则是从 ﻿episodic_notes﻿ 里按当前请求召回 top 3。
这些记忆下一轮是怎么被用到的
    self.memory.remember_file(canonical_path)
    summary = memorylib.summarize_read_result(result)
    self.memory.set_file_summary(canonical_path, summary)
    self.memory.append_note(summary, tags=(canonical_path,), source=canonic
    self.memory.invalidate_file_summary(canonical_path)

===== PAGE 71/167 =====

tests/test_memory.py﻿ 现在已经覆盖了几件关键事：
文件内容变化以后，旧摘要不会继续出现在 ﻿render_memory_text()﻿ 里
pico/metrics.py﻿ 里有 memory dependency experiment 和 large-scale memory experiment。
这两条实验线最关键的地方是，它们不是只做 on/off，对照组还加了 ﻿memory_irrelevant﻿。
在 12 个真实记忆依赖任务里，memory_on﻿ 的重复读取从 ﻿8﻿ 次降到 ﻿3﻿ 次
memory_irrelevant﻿ 没有带来同样收益
Pico﻿ 这版记忆已经够用，但边界也很明确。
这套记忆现在是怎么被验证的
第二类是 memory on/off/irrelevant 实验

===== PAGE 72/167 =====

召回逻辑是轻量规则，不是语义检索
assistant 的普通自然语言回复不会系统性地再提炼回 memory
所以它更像一个轻量工作记忆，不像完整的任务状态板。
这也是当前版本该有的边界。先把减少重复读取和跨轮复用做稳，比一开始就把记忆做成一个复杂黑盒
如果后面真往更复杂的召回走，最需要保住的不是“更高级”，而是读者和开发者还能看懂这条记忆为什
二，把更多高价值结论沉淀进 memory
三，继续保持记忆可解释
Pico 的记忆层没有一上来做成一个很重的会话知识库，我先解决的是重复读取和跨轮复用。现在记忆
旧摘要就会失效；相关记忆召回也没有上重型语义检索，而是先用 tag、关键词、新旧顺序做轻量排
序。这样记忆虽然很轻，但它能稳定回答两个问题：刚才读过什么，哪些结论下一轮还值得继续带着。

===== PAGE 73/167 =====

Pico﻿ 的记忆层做的事情很直接，把跨轮还会继续有用的局部事实留下来，而且尽量保证这些事实不会
prompt 里。对一个本地 coding harness 来说，这一层先把重复劳动降下来，比先做一个很大的记忆黑
实验上这条线也有 on/off/irrelevant 对照，结果说明收益不是因为多塞了点上下文，而是结构化、相
关的记忆真的减少了重复读取。

===== PAGE 74/167 =====

06-上下文瘦身与输出管理设计
问题从 prompt 能不能拼出来，变成了长链路任务里系统怎么把真正有用的上下文留下来。
相关记忆
这里主要给大家讲Pico﻿ 怎么把上下文瘦身做成一条分层机制。
当 agent 在一轮任务里不断读文件、跑命令、积累历史时，runtime 怎么把最有用的上下文继续送给模
history 和 relevant memory 怎么被压缩
代码仓库 agent 的上下文里，有些东西硬度很高：

===== PAGE 75/167 =====

相关记忆
2. 记忆层先做限额
Pico 现在是怎么做上下文瘦身的

===== PAGE 76/167 =====

memory limits

===== PAGE 77/167 =====

这一步的作用很直接，不让完整文件内容直接冲进长期上下文。
06﻿ 这一章不展开记忆设计细节，但它和瘦身是绑在一起的，所以这里必须提。
pico/memory.py﻿ 里现在有几组硬上限：
这层设计解决的是一个很实际的问题，记忆自己不能先长成第二份历史。它得保持小，才能真的帮下一
到了 ﻿ContextManager.build(user_message)﻿，相关记忆会先被召回，再被预算控制。
第二层，memory 自己先限额
第三层，Relevant memory 不全带，只带少量

===== PAGE 78/167 =====

RELEVANT_MEMORY_LIMIT﻿ 当前是 ﻿3﻿。这里还有一个很实用的设计，每条 note 会平分这一段预
代码在 ﻿_render_relevant_memory()﻿：
相关记忆不会全带
selected_notes = self.agent.memory.retrieval_candidates(
    limit=RELEVANT_MEMORY_LIMIT,

===== PAGE 79/167 =====

记忆负责留短摘要，历史负责折叠重复过程。
上下文真的装箱，发生在 ﻿ContextManager.build()﻿。
memory﻿
relevant_memory﻿
memory﻿
relevant_memory﻿

===== PAGE 80/167 =====

先让 ﻿relevant_memory﻿ 收
再动 ﻿memory﻿
这就是 ﻿Pico﻿ 在上下文治理里的核心判断，用户当前这句话最硬，不能失真。
超预算时先裁 ﻿relevant_memory﻿ 再裁 ﻿history﻿
Relevant memory﻿ 最多只保留 3 条
新的上下文比更旧的上下文更容易留下来
DEFAULT_REDUCTION_ORDER = ("relevant_memory", "history", "memory", "prefix"

===== PAGE 81/167 =====

在 12 组长上下文实验配置下，平均 prompt 长度从 ﻿6964﻿ 压到 ﻿5418﻿
第二类是长上下文实验

===== PAGE 82/167 =====

Pico﻿ 的这一层设计，核心是把上下文瘦身做成一条分层机制。
长输出先在入口收，memory 自己先限额，history 按新旧分层，最后再做 section 预算控制。这样 
更多上下文更重要。
Pico 的上下文治理没有走粗暴截断这条路。我做的是分层瘦身。长输出先在入口处裁掉，记忆层自己
缩。超预算时先裁 relevant_memory ，再裁 history ，再裁 memory ，最后才动 
旧日志和重复上下文把当前请求埋掉。

===== PAGE 83/167 =====

本地 code agent 一旦开始处理真实仓库任务，很快就会碰到三件事：

===== PAGE 84/167 =====

memory﻿
工作记忆

===== PAGE 87/167 =====

这个做法很朴素，但很适合 agent 运行过程。

===== PAGE 88/167 =====

build_agent()﻿ 里的逻辑是：

===== PAGE 90/167 =====

如果后面 workspace 和 memory 结构变复杂，恢复时多做几项一致性检查会更稳。
Pico 这里把运行状态拆成了两层。session 负责保存还能继续工作的状态，比如 history 和 memory；
一起，agent 也不只是当场跑完就结束。

===== PAGE 91/167 =====

session 负责延续会话，run artifact 负责记录单轮执行。这样一来，agent 不只会跑，也更容易恢复、

===== PAGE 92/167 =====

如果一个 agent 项目只有模块介绍，没有评测方法，最后很容易掉进一个很尴尬的状态。你能展示它做
成过几次事情，但你说不清这些成功是偶然还是稳定；你也能讲自己做了上下文治理、记忆、工具治理
agent 项目里，怎么把我觉得它还行变成一套有任务合同、有判定标准、有失败解释的评测方法。

===== PAGE 93/167 =====

有工件聚合和实验层。这样最后的数字才有上下文。

===== PAGE 94/167 =====

录，再在副本里启动 agent。

===== PAGE 96/167 =====

这样做的好处很直接，agent 说自己做完了，不代表真的算完成。评测层看的是一组显式条件。

===== PAGE 97/167 =====

这说明 ﻿Pico﻿ 不是只在算结果，也在尽量保住复现上下文。后面你看到一组 benchmark 数字，至少
memory/context/security/provider 这些实验会继续往简历和报告口径走

===== PAGE 98/167 =====

上下文治理更适合看 ﻿prompt_chars﻿、压缩率
记忆层更适合看重复读取和 follow-up 工具步数

===== PAGE 99/167 =====

memory/context/security/provider 结果合并

===== PAGE 100/167 =====

artifact 和 run artifact 聚起来，去回答上下文治理、记忆、安全、provider 这些机制到底有没有收
益。这样最后的数字不会只是一轮演示结果，而是一套带上下文的评测口径。

===== PAGE 102/167 =====

Agent Harness 架构设计：负责本地代码 agent 的整体设计与开发，统一模型接入、工具执行、会
agent 负责决定下一步做什么
harness 负责规定agent在什么边界内做、怎么记录、怎么验证、什么时候才算真的完成。
对于agent工作的时候，harness其实就是围绕模型/agent，去补五类东西：
进、prompt 怎么按结构拼起来，工具怎么显式注册和校验等问题，目的就是想让 agent 放进受控环境
它有工具治理，不是看到工具调用就直接跑，而是先检查工具是否存在、参数是否合法、是不是重复调
它有 memory 和 context manager，解决当前 prompt 里到底什么该进、什么不该进的问题。

===== PAGE 103/167 =====

所以如果把这几层合起来看，我想解决的其实不是模型会不会写代码，而是能不能把一个 code agent 
用户在终端输入命令以后，系统先做启动参数解析，比如工作目录、模型后端、执行预算、审批策略这
这个阶段的目标是把用户的一条命令转换成一个完整的运行上下文。
第二层是上下文构建层。
agent 真正开始推理前，不会直接拿用户原始请求去问模型，而是先把当前系统能提供的上下文整理成
这里我把上下文分成几种不同类型的数据：
稳定上下文，比如工具能力、系统规则、仓库概要
短期工作记忆，也就是当前任务相关的最近文件、最近摘要、临时笔记
相关长期记忆，也就是过去沉淀下来的稳定事实
第三层是 agent 控制循环。（runtime）
1. 构建当前轮上下文  
1.2. 讲一下从输入命令到agent执行完毕，整个pico到运行链路是

===== PAGE 104/167 =====

判断是不是高风险动作，需要不要审批
一类进入短期工作记忆，保证下一轮还能高效利用
它让 agent 系统不只是能跑，还能复盘、调试、评估。
上下文构建层解决“怎么把不同寿命的数据组织成模型可消费的输入”
控制循环解决“怎么让 agent 持续推进，而不是一轮就结束”

===== PAGE 105/167 =====

我在 Pico 里对工具的看法，不是模型需要什么能力，我就给它几个函数，而是把工具当成 agent 系统
所以 Pico 的做法是，所有工具调用都必须先过一层统一的执行网关。
第二，检查是不是重复调用。同一个动作刚做过又来一遍，很多时候说明 agent 已经开始出问题了。
工作区的动作，风险高。高风险动作要受审批策略控制。
agent 一旦做长链路任务，最容易乱的就是状态。
它解决的是下次还能不能接着干。这里面放的是会话历史、工作记忆、长期记忆这些会跨轮延续的内
这一层专门服务恢复。它保存的是如果现在上下文超了，或者运行被打断了，下次从哪继续的短状态。

===== PAGE 106/167 =====

用、有没有工具失败、有没有恢复判错；如果是上下文问题，还能继续看这轮 prompt 是怎么组出来
个机制到底有没有带来收益，比如上下文治理会不会真的把 prompt 压下来，记忆会不会减少重复读文
比如如果恢复实验里某类场景指标不好，我就可以回到具体运行工件去看，到底是旧状态没识别出来，

===== PAGE 107/167 =====

长上下文治理：设计分层上下文管理与预算裁剪机制，在 12 组长上下文配置里，将平均 prompt 长
具体是在解决一个具体的问题：多轮任务一长以后，agent 每一轮到底该看到什么，不该看到什么。
因为 code agent 和普通聊天不一样，它不是回答一句话就结束了，而是在一个真实仓库里持续做判
第一个问题是上下文越来越胖。很多旧信息其实只是某一轮临时有用，但它会一直留在后面的 prompt 
第二个问题是信息混层。稳定背景、当前任务状态、历史过程、相关记忆全混在一起，模型每次都像在
净的一份上下文上做判断。换句话说，我治理的不是长度本身，而是上下文结构和优先级。
最后这个设计会直接影响 agent 的三个能力。
Pico 里的上下文，我不是把所有东西拼成一段，而是故意拆成几层。
这里放的是每一轮都需要的东西，比如 agent 的角色、工具怎么调用、当前仓库的一些稳定信息。这部
2. 上下文治理
2.1. 上下文治理具体在治理什么
2.2. 上下文是怎么分层的，为什么要这样分

===== PAGE 108/167 =====

第二层是工作记忆。
第三层是相关记忆。
这层不是所有记忆都进，而是针对当前这轮请求，挑出少量最相关的内容带回来。这样做的好处是，记
忆不会整体撑大上下文，而是按需回来。
这层保留的是执行过程，包括前面几轮问答、工具调用和中间步骤。它当然有价值，但它更偏“过程证
这层我会单独放，而且必须完整保留。因为不管上面有多少背景、记忆和历史，这一轮模型最后到底要
这样分的原因是只有分层以后，系统才能在预算不够的时候做正确的取舍。比如相关记忆可以先收，历
史可以压缩，但当前请求不能丢。再比如稳定背景适合复用，工作记忆适合滚动更新，过程历史适合按
上下文分层的目的，不是为了结构好看，而是为了让系统知道每一类信息该怎么保留、怎么裁、怎么复
比如相关记忆可以先收，因为它是按需补充的；

===== PAGE 109/167 =====

工作记忆再往后收，因为它已经更贴近当前任务；
这是按几个最直接影响上下文压力的变量组合出来的。我当时想的是，长上下文问题本质上不是一个单
也就是这轮前面到底已经积累了多少过程信息。历史短的时候，上下文压力和历史长的时候完全不是一
第二类是记忆负载。
包括工作记忆和相关记忆里到底有多少内容要带回来。因为有的任务历史不长，但记忆层很重，照样会
2.4. 这 12 组长上下文配置是怎么设计出来的

===== PAGE 110/167 =====

大致上是：历史做几档、记忆负载做几档、请求长度做几档，最后组合出 12 组不同压力条件。
这样测出来的不是某个单点结果，而是这套上下文治理机制在不同上下文压力下的整体表现。
这 12 组配置把历史长度、记忆负载和请求长度这几个主要压力维度系统地覆盖到了。
Pico 这套上下文治理追求的不是压得越狠越好，而是在不破坏当前任务语义的前提下，把冗余部分收
如果我只追求压缩率，最简单的方法其实是更激进地裁历史、裁记忆，甚至把当前请求也压一下。这样
数字会更漂亮，但 agent 在真实任务里很可能直接失真。
对本地 code agent 来说，这种高压缩率没什么意义。
如果上下文本来就不长，可压缩空间自然有限。真正更能说明机制上限的，是最长那几组配置下的结
果。这也是为什么我会同时看平均压缩率和最高压缩率。最高到 33.28%，说明在上下文压力真正上来

===== PAGE 111/167 =====

它没有为了追数字去牺牲稳定性，而是在长任务里稳定地释放了一部分上下文预算。
对 code agent 来说，这种收益比追一个特别夸张、但很脆弱的压缩率更有实际价值。
结构化记忆系统：针对多轮任务里 agent 反复读同一文件、重复确认已知事实的问题，把任务摘要、
文件摘要、过程笔记和相关记忆召回做了分层；在 12 个记忆依赖任务里，follow-up 阶段的重复读
文件次数从 60 次降到 0 次，且不再需要额外工具调用去重新确认已经拿到的事实。
因为 code agent 在真实仓库里做多轮任务时，信息其实不是一种东西。
所以 Pico 里我把记忆分成了几层去处理。
最靠近当前任务的是工作记忆，里面放任务摘要、最近接触过的文件、文件短摘要和少量过程笔记；
更稳定一点的事实，单独作为长期记忆沉淀下来；
而真正跟恢复强相关的当前状态，我不放在记忆层里，而是放在 checkpoint 里。
3. 记忆系统
3.1. 结构化记忆指什么

===== PAGE 112/167 =====

Pico 现在做的，就是把记忆从模糊的上下文堆积变成“裁剪、可召回、可失效、可恢复配合的运行时部
让 agent 在多轮任务里记得住、拿得准、也不会越记越乱。
有很多上下文不等于上下文有用。
第一个问题是上下文膨胀。
这些东西留着当然有价值，但不是每一轮都该带进模型。你如果不分层，最后 agent 每轮都要重新读一
稳定背景放一层，工作记忆放一层，相关召回放一层，历史过程再放一层，当前请求永远单独保留。
3.2. 为什么记忆要分层，而不是把历史或摘要直接塞进 prompt

===== PAGE 113/167 =====

相关记忆召回这块，我的设计重点没有放在做一个很重的语义检索系统上，而是先把这一轮最该带什这
Pico 里，一轮新的请求进来以后，不是把所有记忆全塞回 prompt，而是先去思考：这次任务到底跟哪
然后系统再从工作记忆和长期记忆里，挑少量最相关的内容带回来。
很久以前的旧结论。这样做的好处是可解释。后面如果我想知道为什么这一条记忆被召回了，我是能说
这里我刻意没有一上来做很重的语义检索，原因有两个。
第一，Pico 现在的体量还没大到必须上向量检索那一步；
从上下文组织上看，我把它放在一个单独的相关记忆层里，而不是混到工作记忆和历史里。
稳定背景、工作记忆、相关记忆、过程历史、当前请求。
对 code agent 来说，过期的记忆比没有记忆更危险。没有记住，最多就是多读一遍文件；但如果记住
的是旧结论，还继续拿它往下推理，那后面整条链路都可能偏掉。
所以 Pico 里我专门做了一层记忆和真实工作区对齐的约束。最典型的就是文件摘要不会只存一句话，它
还会绑定到具体文件，并且带一个新鲜度标记。这样下一轮系统在准备上下文时，不是直接把旧摘要拿
3.3. 相关记忆召回是怎么做的，怎么决定这一轮带什么
3.4. 怎么保证记忆不过期，不会拿旧事实继续推理从而产生错误

===== PAGE 114/167 =====

这样做把记忆从静态缓存变成受工作区约束的缓存。
也就是说，记忆从来不是绝对真相，它只是对某一时刻真实状态的压缩表示。
不管是记忆还是恢复，它们都不能脱离当前活跃的工作去单独存在。
Pico 不是在追求记得越多越好，而是在追求记住的东西尽量别过期；因为对 code agent 来说，错用旧
我设计记忆实验时，重点看的不是总成功率，而是 follow-up 阶段两个更贴近执行过程的指标：
因为这两项更直接反映 agent 有没有把已经确认过的事实真正留下来。
同一批记忆依赖任务，分成三组跑：
一组开记忆，一组关记忆，还有一组保留记忆框架但塞无关内容。
这样就能区分：收益到底是来自多塞了一点上下文，还是来自结构化、相关的记忆真的在工作。
最后跑出来的结果是，开记忆之后，在 follow-up 阶段重复读文件从 60 次降到 0，而且不再需要额外
工具调用去重新确认已经确认过的事实。
不该再读第二遍的那种依赖任务。所以它不是说明 agent 在所有任务里都会这样，而是说明在依赖前面
已确认事实的场景里，结构化记忆确实把重复劳动压掉了
第一，记忆收益不是多带点上下文这么模糊，而是能具体体现在执行过程上；
第二，Pico 的记忆层不是为了让模型显得更聪明，是为了让 agent 在多轮任务里少绕弯路。
我验证记忆层时，重点不是看它让模型多答对了几题，主要看它有没有让 agent 少做已经做过的事。
对 code agent 来说，这才是记忆最直接、也最值钱的收益。
3.5. 你怎么证明这套记忆真的有用？为什么重复读文件能降到 0

===== PAGE 115/167 =====

任务恢复机制：设计 checkpoint / resume 机制，让 agent 在上下文超预算、中断恢复和 
漂移识别率 100%，且没有出现误信旧状态继续执行的情况。
对code agent 来说，恢复真正危险的地方不是恢复失败，而是恢复错了还继续跑。所以我不是从任务类
因为对 code agent 来说，恢复最危险的不是恢复失败，而是恢复错了还继续跑。所以我不是按任务类
始。这里重点看两件事：目标能不能带回来，关键文件上下文能不能带回来。
运行条件变了。重点看系统能不能把这种情况识别成 mismatch，而不是装作没事继续做。

===== PAGE 116/167 =====

对 code agent 来说，真正重要的不是上一次聊过什么，而是现在做到哪了，下一步该往哪走。
合 working memory 和 freshness 校验来决定哪些状态还能继续信。
prompt 的时候，ContextManager 不会把旧 prompt 直接重用，而是重新组装一份新的上下文，把 
最终把 agent 从我知道我们聊过很多东西切回我知道这件任务现在做到哪了，而且我知道接下来该怎么
checkpoint 在 Pico 里，本质上是恢复入口。专门解决一个问题：如果上下文太长了，或者运行被打断
checkpoint和 memory 的区别，在于checkpoint 是为了恢复，memory 是为了复用。
4.3. checkpoint是怎么设计的，和 memory 的区别是什么

===== PAGE 117/167 =====

memory 更像工作记忆，它保存的是下一轮大概率还会继续用到的内容，比如任务摘要、最近接触过的
文件、文件短摘要、过程笔记，还有后面加的轻量长期记忆。它的作用是减少重复劳动，让 agent 不要
状态。像当前目标、当前卡点、下一步动作，这些如果长期放到 memory 里，后面就会和 checkpoint 
抢职责，系统会越来越乱。所以我在设计上故意把它们拆开：让memory 管下一轮可能有用的信息，
让模型重新消化一大段过程信息。对 code agent 来说，这个成本会越来越高，而且里面很多内容对接
已经整理好的短状态，也就是 checkpoint，再结合工作记忆和新鲜度检查，判断哪些内容还能继续用。

===== PAGE 118/167 =====

比如当前工作目录变了、工作区整体指纹不一样了、模型或执行策略变了、只读模式和审批策略变了，
生成新动作了。对 code agent 来说，这比直接失败更麻烦。因为它可能会继续读错文件、改错位置、

===== PAGE 119/167 =====

我自己理解，code agent 最危险的点主要有三类。
具，它等于把系统能力暴露给模型。如果没有审批策略和风险边界，模型只要一次判断失误，代价会很
很多 agent 真正麻烦的地方不是这次命令报错了，而是命令报错了、工作区却已经变了，系统还当成普
5.2. 工具怎么分风险等级，审批策略怎么设计

===== PAGE 120/167 =====

范围最大，所以我在设计上会把 shell 默认看成最需要审批和约束的一类。
审批策略这块，我做的不是所有高风险动作都弹窗，而是按运行模式去控制。
在需要人工确认的模式下，高风险动作要先过审批。
另外我觉得审批策略不能只和工具名绑定，还要和参数内容绑定。
参数校验我是这么做的：不把模型吐出来的工具调用直接当成可信输入。因为模型输出本质上还是文

===== PAGE 121/167 =====

工作区隔离是为了保证 agent 再怎么工作，也只能在当前仓库边界内活动。

===== PAGE 122/167 =====

真正去看上下文治理、记忆收益和恢复正确性，我会看另外几组对照实验，不会都靠这一层来证明。
评测与审计闭环：将评测拆成 harness regression、上下文治理、记忆收益和恢复正确性几层，分别
这里我会记录每一轮上下文是怎么组出来的、模型什么时候被调用、输出被解析成了什么、工具有没有

===== PAGE 123/167 =====

比如最终答案、关键指标、这轮 prompt 的大小、恢复状态、有没有命中过记忆、有没有发生安全事
如果我要看上下文治理，我就看 prompt 平均长度、压缩率和当前请求有没有被裁坏；
如果我要看记忆收益，我就看重复读文件次数、follow-up 阶段的工具步数；
如果我要看恢复正确性，我就看恢复成功率、workspace 漂移识别率，以及有没有误信旧状态继续执
到底是任务合同定义得有问题，还是中间工具调用被拦了，还是上下文裁剪出了问题，还是恢复时把旧

===== PAGE 124/167 =====

这是一个面向本地coding场景的轻量code agent。
我之所以会自己做一个 code agent，主要有两个很现实的原因。第一是可控性。平时我也会用 Claude 
Code 这类产品，但很多关键东西对用户来说其实是黑盒，比如 prompt 是怎么拼的、上下文是怎么组装
要求。实验室有些项目本身有保密约束，不适合直接放到外部闭源服务里做，所以我也希望这套 agent 
在设计上我没有把重点放在“接个模型、配点工具”这种 demo 上，而是重点去解决 coding agent 真正
题之后怎么恢复和复盘。围绕这些点，我做了显式任务状态、分层记忆和预算感知的上下文组装、工具
审批机制，以及 session 和 run 两层持久化，最后还配了一套固定任务集和离线 evaluator 去做可复现
从结果看，pico。上下文治理在 12 组配置里把平均 prompt 长度从 6964 压到 5418；记忆实验里，重
agent 负责决定下一步做什么

===== PAGE 125/167 =====

harness 负责规定agent在什么边界内做、怎么记录、怎么验证、什么时候才算真的完成。
对于agent工作的时候，harness其实就是围绕模型/agent，去补五类东西：
进、prompt 怎么按结构拼起来，工具怎么显式注册和校验等问题，目的就是想让 agent 放进受控环境
它有工具治理，不是看到工具调用就直接跑，而是先检查工具是否存在、参数是否合法、是不是重复调
它有 memory 和 context manager，解决当前 prompt 里到底什么该进、什么不该进的问题。
所以如果把这几层合起来看，我想解决的其实不是模型会不会写代码，而是能不能把一个 code agent 
用户在终端输入命令以后，系统先做启动参数解析，比如工作目录、模型后端、执行预算、审批策略这
这个阶段的目标是把用户的一条命令转换成一个完整的运行上下文。
2. 讲一下从输入命令到agent执行完毕，整个pico到运行链路是

===== PAGE 126/167 =====

第二层是上下文构建层。
agent 真正开始推理前，不会直接拿用户原始请求去问模型，而是先把当前系统能提供的上下文整理成
这里我把上下文分成几种不同类型的数据：
稳定上下文，比如工具能力、系统规则、仓库概要
短期工作记忆，也就是当前任务相关的最近文件、最近摘要、临时笔记
相关长期记忆，也就是过去沉淀下来的稳定事实
第三层是 agent 控制循环。（runtime）
1. 构建当前轮上下文  
判断是不是高风险动作，需要不要审批

===== PAGE 127/167 =====

一类进入短期工作记忆，保证下一轮还能高效利用
它让 agent 系统不只是能跑，还能复盘、调试、评估。
上下文构建层解决“怎么把不同寿命的数据组织成模型可消费的输入”
控制循环解决“怎么让 agent 持续推进，而不是一轮就结束”
Pico上下文管理的核心思想是，把上下文当成一个有预算、有优先级的运行时状态来管理，这块的设计
主要解决的问题是是本地 coding agent 在多步任务里，怎么让模型每一轮都看到一份够用、稳定、不过
载的上下文
第一层是稳定上下文，也就是 prefix。这部分不是会话历史，而是 agent 每一轮都要用到的固定背景。
段。这么做的原因很直接，本地 coding agent 和聊天不一样，它不是只回答一句话，它要在一个仓库里
上下文稳定下来。这样后面 history 变了，这层也不会跟着抖，而且还能做 fingerprint 和缓存复用。
第二层是 working memory，也就是轻量工作记忆。这里我设计得比较克制。只保留几类下一轮大概率
3. Pico里面的上下文是怎么管理的

===== PAGE 128/167 =====

notes。核心思想是也尽量减少Agent的重复劳动。比如我刚读过哪个文件，这个文件前几行大概讲了什
么，如果这些信息如果不留下来，下一轮就很容易再读一遍、再找一遍。working memory 的作用就是
随手拼接一下，而是明确拆成五段：prefix、memory、relevant memory、history、current request。
的记忆，再给历史，最后把当前用户请求原样放在最末尾。这个顺序的安排是关注模型这一轮最先该看
这五段都有预算。上下文超了以后，Pico 不是粗暴截断，而是按优先级收缩，先收 relevant memory，
再收 history，然后才动 memory 和 prefix，current request 永远不裁剪。这里其实就是在处理一个很
实际的问题：上下文不够的时候，哪些信息可以缩，哪些信息不能丢。Pico 的做法很明确，补充信息可
最后我总结一下，Pico 的上下文管理就是把稳定背景、短期工作记忆、相关记忆和过程历史分开，再用
Pico 的记忆设计，核心思想是只记真正有用的东西，并且把记忆分层。它要解决的其实是两件事，一件
淀下来，后面还能复用。同时还得控制一个问题，就是这些内容不能把上下文挤爆。
第一层是当前工作记忆，解决的是这件事怎么继续往下做。这里面主要放当前任务摘要、最近看过的文
第二层是长期记忆，解决的是这个项目里哪些稳定事实值得留下来。比如用户偏好、项目约定、关键决
策、依赖要求、反复出现的坑，这些会单独落到 .pico/memory 下面。Pico 不是什么都存。像临时尝
试、这一轮失败、当前报错这种短期信息，它不会轻易升成长期记忆。也就是说，我们明确把过程信息
和长期知识分开了，不让长期记忆变成另一份冗长日志。
再往下一层看，Pico 也不是把这些记忆一股脑全喂给模型，而是会先挑和当前问题最相关的那几条，再
结合上下文预算去组装。重点不是让模型记得越多越好，而是让它每一轮都尽量站在真实仓库状态上做
继续拿过期信息推理。很多 agent 会记很多内容，但不太处理这些内容是不是已经过期。Pico 在这块是
4. Pico的记忆设计是怎么样的

===== PAGE 129/167 =====

我总结一下，Pico 的记忆设计，不是在追求记忆越大越强，而是在追求记忆够用、相关，而且不过期。
第二，这个项目的编排逻辑并不复杂，还没复杂到必须引入图编排框架。
第三，我希望 prompt、工具协议、审批策略、trace 格式都能完全按自己的方式定义，避免为了适配框
架抽象而增加额外复杂度。等后面任务流变复杂，比如多节点状态机、可视化编排、长期工作流复用明
Agent最容易出问题的地方，往往不是最后那一下，而是中间怎么走的。它可能来回读一堆没用的文件，
择是否合理这些都沉淀成可量化指标。这样以后评测的就不只是有没有做成，还包括是不是用一条靠谱
第二步，我会补这套系统的边界管理，重点解决 agent 什么能做、什么不能做的问题。现在 pico 已经
的 Agent harness 来看，这一层还可以再独立一点。我的想法是，把这部分单独做成一套规则系统，明
5. 为什么不直接用现成 agent framework

===== PAGE 130/167 =====

分析的程度。比如我想看为什么这次比上次更慢，为什么某一轮上下文裁剪之后效果变差了，或者为什
code agent 的难点，不只是生成代码本身，而是在一个持续变化的真实系统里，稳定地做出可落地的修
态、已有实现约束、依赖版本、测试假设、文件之间的耦合关系，也取决于前面几步工具调用拿回来的
所以第一个挑战，是怎么把系统状态表示清楚。agent 到底看到了什么，哪些是当前任务最相关的信
较克制，它没有把整段对话历史都塞回去，而是把工作区基线、当前工作记忆、长期记忆和历史记录拆
第二个挑战，是修改动作要尽量准确。很多代码 agent 出错，不是不会写，而是改不准，改错了位置，
第三个挑战，是多步执行过程本身要稳。代码 agent 不是一次性生成，而是一个循环过程，先读，再判
用同一个工具，拿过期信息继续推理，或者还没验证就提前说自己做完了。Pico 在运行时里会记录尝试
8. 主流agent框架改进的方向
9. code agent生成代码有什么挑战

===== PAGE 131/167 =====

次数、工具调用步数和停止原因，也会拦住重复的相同调用。这里体现的是一个很重要的系统设计思
路：agent 不只是能力问题，运行过程本身也需要治理。
最后我总结一下，code agent工程化上困难的地方不在把代码写出来，而在把代码生成放进一个有状

===== PAGE 132/167 =====

第一点，agent loop 本质上应该被当成状态机来设计，不是普通 REPL。Claude Code 会显式管理继续
原因、终止原因、恢复路径、上下文压缩和失败降级。这个思路对我影响很大，因为它把问题从模型回
在的工具是显式白名单注册，参数会严格校验，patch_file 要求精确命中一次，风险工具要经过审批，
delegate 出去的子 agent 默认是只读的。这些设计看起来没那么炫，但我觉得这才是 agent 工程能力
第三点，上下文管理不是拼 prompt，而是做预算。Claude Code 很强的一点，是它把上下文当成受限
memory、relevant_memory、history、current_request 这几段，每段有预算和收缩顺序，而且永远不
非常重要，因为 coding agent 最大的风险，很多时候不是写坏代码，而是认真写了不该写的东西。这个
点 pico 还没有完整实现，我不会夸大，但我们已经有前置积木，比如审批模式、只读 delegate、

===== PAGE 133/167 =====

0417字节Agent开发一面
1. 为什么要自己搞一个code agent？这个过程中你觉得哪部分是比较有挑战

===== PAGE 134/167 =====

为什么要做code agent：一个参考的包装环境，可以根据自己的背景和项目来
如 prompt 是怎么拼的、上下文是怎么组装的、工具在什么条件下会被调用，我很难真正判断它为什
我也希望这套 agent 从工具到模型都能自己控制。我在项目里预留了接开源模型的能力，比如可以直
1. 上下文工程：context 是有限资源，怎么解决长仓库任务里逐渐偏离最初 spec，比如忘记约束、
误用旧信息、重复操作、或者把局部修复做成全局破坏等上下文还没超窗时，模型对长历史的利
2. 工具设计：agent 的效果取决于给它什么工具，而且哪怕只是 tool description 和 schema 的小
https://www.anthropic.com/engineering/code-execution-with-mcp?
3. harness 设计：怎么让 agent 跑起来之后，不至于失控、卡死、跑偏、忘事、无法追责。随着任
务跨越多个 context window，光靠模型单轮推理已经不够，必须有一个外层 harness 去管理进
度、状态、记忆、验证和恢复。（可以看下这篇：
2. 那你自己用的code agent，用过哪些，哪个最好用？有没有用过字节的

===== PAGE 135/167 =====

个人建议一定要回答到claude code，如果你做code agent连claude code都没用过相当于你写java
觉claude code最好用，它的产品形态和思维能力都很强，这跟他的code agent设计以及模型的强大
从我实际使用的角度来看，了解每个code agent的特点，清楚他的边界，再组合使用，会让我的任务
解决变得高效。我的整个code agent介入下的工作流目前是：
新./AGENTS.md 的bad case -> 再用Claude Code reivew -> codex进度管理，完成git commit 和
如果要举个例子来概括我用这些 code agent 的感受，我觉得自己更像一个工程团队的 TL。好的 TL 
用 code agent 其实也是一样。你得了解每个 agent、每个模型擅长什么，不擅长什么，再把合适的
1. 做垂类产品，应聘不同公司，一定要了解对方在这个垂类上的产品，比如你做code agent，去应

===== PAGE 136/167 =====

3. 有没有观察过同一个模型在不同code agent的表现？你觉得为什么会有不

===== PAGE 137/167 =====

这是个很有意思的问题，面试官用这种你日常使用场景里的小细节来判断你对code agent这个领域的
我觉得同一个基础模型放在不同 code agent 里，表现差异往往非常明显，而且很多时候差异并不主
要来自模型本身，而是来自 agent 框架对任务的拆解、上下文组织、工具调用、反馈闭环和执行约束
第一，任务编排不一样。比如有的 agent 会先做任务理解、列计划、定位相关文件、再逐步修改；有
的 agent 则更偏一次性生成。对于多文件修改、需要理解仓库结构的问题，
第二，上下文构造不一样。虽然底层模型一样，但不同 agent 给模型喂进去的信息不同，比如 
claude code的CLAUDE.md和codex的AGENTS.md的差异）
第三，工具链和反馈机制不一样。code agent的工具设计很重要，搜索、grep、代码索引、运行测
试、lint、执行 shell、读写文件等工具，各家是有差异的。一个 agent 如果能在改完代码后自动跑测
试，并把失败栈回灌给模型继续修，就会比只生成代码不验证的 agent 明显更强。也就是说，agent 
第五，约束和安全边界不一样。有些 agent 对改动范围、命令执行、依赖安装、网络访问限制更严
格，这会让它在某些任务上显得保守，但也更可控。另一些 agent 更激进，短期通过率可能更高，但
所以我会认为：同一个模型在不同 code agent 上表现不同，是一个很正常的系统现象。模型决定了
上限，但 agent 设计决定了它能发挥出多少。
如果面试官问你会怎么优化一个 code agent，可以参考这么个丝路：
第一，提高代码检索和上下文压缩质量，确保模型看到对的代码；
第三，控制单次改动粒度，避免大范围误改，并给 agent 明确的停止条件和回滚策略。

===== PAGE 138/167 =====

code agent在现在这个时间点完全避开不了claude code，特别是源码泄漏之后，大家需要好好准
第一点，agent loop 本质上应该被当成状态机来设计，不是普通 REPL。Claude Code 会显式管理
继续原因、终止原因、恢复路径、上下文压缩和失败降级。这个思路对我影响很大，因为它把问题从
要经过审批，delegate 出去的子 agent 默认是只读的。这些设计看起来没那么炫，但我觉得这才是 
agent 工程能力真正站得住的地方，因为它决定了系统在边界情况里会怎么表现。
第三点，上下文管理不是拼 prompt，而是做预算。Claude Code 很强的一点，是它把上下文当成受
memory、relevant_memory、history、current_request 这几段，每段有预算和收缩顺序，而且永远
这件事非常重要，因为 coding agent 最大的风险，很多时候不是写坏代码，而是认真写了不该写的
东西。这个点 pico 还没有完整实现，我不会夸大，但我们已经有前置积木，比如审批模式、只读 

===== PAGE 139/167 =====

agent 负责决定下一步做什么
harness 负责规定agent在什么边界内做、怎么记录、怎么验证、什么时候才算真的完成。
对于agent工作的时候，harness其实就是围绕模型/agent，去补五类东西：
推进、prompt 怎么按结构拼起来，工具怎么显式注册和校验等问题，目的就是想让 agent 放进受控
它有工具治理，不是看到工具调用就直接跑，而是先检查工具是否存在、参数是否合法、是不是重复
它有 memory 和 context manager，解决当前 prompt 里到底什么该进、什么不该进的问题。
agent 做成一个有状态、有边界、有验证、也有回归能力的工程系统。这也是为什么我最后会把 
agent的harness有哪些挑战？

===== PAGE 140/167 =====

code agent 的核心难点，在把模型放进一个可控、可验证、可追责的执行系统里，harness 解决的
还给人。这里的难点不在过程控制。模型擅长局部推理，但不擅长长期、稳定地管理执行状态，所以
编排上必须在 harness 这一层完成。
第二，状态和记忆怎么管。长任务一定会遇到 context rot，也一定会出现上下文膨胀。harness 要决
定哪些信息进入当前上下文，哪些压缩成 summary，哪些写入持久 memory，哪些应该直接丢弃。
否则 agent 很容易做着做着忘掉最初约束，或者重复已经做过的步骤。
有 verifier，agent 只是会写代码；有 verifier，它才接近能交付结果。
第四，执行隔离和权限控制怎么做。只要 agent 能写文件、跑命令、访问网络，就一定会带来安全风
险，包括误操作、越权操作和 prompt injection。harness 必须把自动化建立在 sandbox、
第五，并发和任务拆分怎么做。一个任务到底应该由单 agent 完成，还是拆成多个 subagent 并行处
理。并行确实能提升速度，也能让不同 agent 分工处理搜索、修改、验证这些子问题，但代价是编排
的成本更高，结果更难合并，一致性更难保证。harness 必须清楚定义拆分边界、上下文继承方式，
第六，可观测性和可追责怎么做。真实系统里，你必须知道 agent 为什么改了这个文件，执行过哪些
命令，在哪一步失败，是否触发过人工审批，这一版 patch 比上一版到底好在哪里。没有 trace、
还是编排逻辑。可观测性不是补充项，而是 harness 能不能持续演进的前提。
6. 讲一下你项目里面用户在终端发一个指令到agent完成所有任务的整个链路

===== PAGE 141/167 =====

用户在终端输入命令以后，系统先做启动参数解析，比如工作目录、模型后端、执行预算、审批策略
这个阶段的目标是把用户的一条命令转换成一个完整的运行上下文。
第二层是上下文构建层。
agent 真正开始推理前，不会直接拿用户原始请求去问模型，而是先把当前系统能提供的上下文整理
这里我把上下文分成几种不同类型的数据：
稳定上下文，比如工具能力、系统规则、仓库概要
短期工作记忆，也就是当前任务相关的最近文件、最近摘要、临时笔记
相关长期记忆，也就是过去沉淀下来的稳定事实
第三层是 agent 控制循环。（runtime）
1. 构建当前轮上下文  

===== PAGE 142/167 =====

判断是不是高风险动作，需要不要审批
一类进入短期工作记忆，保证下一轮还能高效利用
它让 agent 系统不只是能跑，还能复盘、调试、评估。
上下文构建层解决“怎么把不同寿命的数据组织成模型可消费的输入”
控制循环解决“怎么让 agent 持续推进，而不是一轮就结束”
7. 怎么减少这个链路里面Agent的幻觉？

===== PAGE 143/167 =====

pico对于减少 Agent 幻觉不是做单点的优化，而是分层多体系的优化。
库、执行边界、审批策略、是不是继续旧 session。这一步本质上是在减少模型自行补全任务定义的
第二，上下文层做信息治理，减少脏上下文。
很多时候幻觉的产生是因为模型在无中生有，而是它拿着过期信息、无关信息、冲突信息继续推理。
pico在这里的设计重点不是给更模型干净、分层的（或者说更有结构的）上下文
稳定规则、短期工作记忆、相关长期记忆、会话历史、当前请求，这几类信息是分开管理的，而且当
在 pico 里，一次任务是一个多轮 runtime。每一轮都会先重建当前上下文，再让模型做一个非常受
第二，工具执行结果不会停留在那一轮里，而是会回写到 history、working memory 和 task 
state，变成下一轮推理的输入。agent 每走一步，都会用真实执行结果刷新自己对当前仓库状态的理
够证据，再收敛成最终的答案。这套控制循环其实是在系统层面把 agent 的默认行为从语言补全改成
pico 在执行层有一个统一网关，负责校验动作是否合法、是否重复、是否越权、是否需要审批，然后
长任务里最容易出现的是状态幻觉，也就是外部世界已经变了，但 agent 还按旧状态继续推进。所

===== PAGE 144/167 =====

如果没有 trace、report、task state，你根本不知道这次错误到底是出在上下文污染、动作失败、状
态漂移，还是模型自己瞎推理。所以 pico 会把过程、结果和关键状态变化都记录下来。只有可观
我对 pico 里减少 Agent 幻觉的理解是通过任务收敛、上下文治理、证据优先、执行隔离、状态对齐

===== PAGE 145/167 =====

这一层先定义 agent 的工作边界。里面会放几类信息：
一类是 agent 自己的身份和输出协议，比如它必须返回工具调用还是 final answer；
第二层，是工作记忆。
pico 不会把完整会话历史直接塞回去，而是先提炼一层 working memory。
这样做的目的，是把上下文从“聊天记录”变成“任务现场”。
第三层，是相关记忆召回。
除了固定的 working memory，pico 还会围绕当前用户请求，再额外召回少量最相关的 note。

===== PAGE 146/167 =====

原因很简单：不管前面放了多少上下文，最终这轮推理还是要对齐到“现在要做什么”。
先压相关记忆，再压历史，再压工作记忆，最稳定的前缀最后才动，而当前请求基本不动。
模型最不能丢的是规则边界和这轮任务本身，最可以牺牲的是一部分可替代的上下文细节。
所以我理解 pico 的 prompt 构建，本质上不是在“组一段长文本”，而是在做上下文分层治理。
前缀负责定义边界，工作记忆负责保留任务现场，相关召回负责把过去真正有用的信息拉回来，压缩
9. 讲一下你的code agent的memory设计，有没有做提示词缓存？然后问了简

===== PAGE 148/167 =====

Pico 的记忆设计核心是是让模型在多轮任务里别反复做已经做过的事。
因为对 code agent 来说，代码仓库里的信息其实不是一类东西。
像这个方向试过了，不成立、刚才那个补丁只做了一半，先看 diff 再继续，这种更像 agent 执行过
最靠近当前任务的是工作记忆。实现上，我在运行时的内存状态里专门留了几块：任务摘要、最近接
记住了就算完。下一轮组装上下文时，系统会先检查这个文件现在是不是还是上次那个版本。如果文
旧摘要作废。因为对 code agent 来说，没记住最多就是多读一遍文件；但如果记住的是旧结论，还
息。召回的时候，我也没有一上来就上很重的语义检索，而是先用比较透明的规则：先看标签有没有
我要分析为什么这条记忆被带回来了，我能说清楚原因，而不是只能说模型觉得它相关。
再往上一层，我还做了长期记忆。因为有些内容不应该一直塞在当前会话的工作记忆里，比如项目约
y/﻿ 下面，用一个索引文件加若干主题文件去存。这样工作记忆可以保持很小、很贴当前任务，长期
前任务状态、下一步、关键文件、工作区身份这些恢复时必须信的内容；记忆里放的是下一轮可能有

===== PAGE 149/167 =====

上下文组装的时候，我也不是把所有东西一股脑塞进去，而是按层次放：稳定背景、工作记忆、相关
记忆、过程历史、当前请求。预算不够时，会优先压缩相关记忆和旧历史，当前用户这一轮的新请求
再把完整工具输出塞进去。说得更直白一点，我不是在追求信息越多越好，而是在做上下文预算管
提示词缓存这块，Pico 里也有实现，但它跟memory还是有区别的。记忆解决的是多轮任务里别重复
简历上的指标，对应的是我专门做的一组记忆依赖实验。实验一共设计了 12 类任务模板，每种设置
重复跑 5 次，所以每种方案一共是 60 次 follow-up。对照组分三组：开记忆、关记忆、保留记忆框
架但故意塞无关内容。最后结果很干净，开记忆以后，follow-up 阶段的重复读文件次数从 60 次降
到 0 次；关记忆和塞无关内容，都是 60 次。与此同时，平均工具步数从 1.0 降到 0，平均尝试次数
从 2.0 降到 1.0，正确率保持不变。这个结果说表明是结构化记忆真的把前面已经确认过的事实记住
了，所以 agent 在 follow-up 阶段不需要再调用工具去重新确认。
Pico 的记忆设计，本质上是在把有用的信息从完整的历史里拆出来，再给这些信息加上寿命管理和
失效校验。这样 agent 在多轮任务里既记得住，又不会越记越乱。

===== PAGE 150/167 =====

任务恢复主要解决 agent 上次做到一半停下了，这次要尽量接着那个任务状态继续做的问题
这里的难点是，Agent不能盲信旧状态。因为代码仓库是活的，文件会变，工作区会变，运行配置也
外，我还会把一份运行身份一起存进去，比如当时的工作目录、模型、审批策略、只读开关、最大步
份是不是还对得上。比如文件内容变了，或者工作区指纹变了，或者模型、审批策略、功能开关变
我还自己设计了一个小心思，就是恢复和记忆是连在一起的。比如工作记忆里存过某个文件的短摘
Pico 的恢复会先确认哪些旧信息还有效，再决定接多少。这点对 code agent 很重要，因为没记住，
最多是多读一遍；但如果记住的是旧事实，还拿它继续推理，后面整条链路都会偏掉。（很危险的场

===== PAGE 151/167 =====

目前这个项目还不支持skill 和 MCP。它现在更像一个比较轻量的 coding agent，核心能力是内置工
所以它已经有 agent 的基本闭环了，但还没有做到能力可以被外部发现、加载和扩展这一步。
如果要把 skill 和 MCP 引进来，我会分别设计。
MCP 更适合放在工具和能力提供这一层，解决的是模型能调用什么。
第三步，再加一个 MCPToolProvider，把 MCP server 暴露出来的 tools 映射成系统内部统一的工
具定义，这样现有的审批、trace、timeout、错误处理这些机制都还能复用，不需要重写一套。
这样做的好处是，skill 和 MCP 的职责是清楚的，核心 runtime 也不会被污染，而且整个演进路径是
渐进的，不需要一上来重构成一个很重的平台。第一阶段我会先做 MCP tools，不会急着把 
目前项目还不支持 skill 和 MCP，它现在只有静态内置工具。要加的话，我会把 skill 放到 
prompt/resource 层，把 MCP 放到 tool provider 层。落地顺序是先重构工具注册，再加 skill 
registry，最后接 MCP tool provider。这样既能复用现有的安全和执行链路，也不会把核心 
12. 现在你这个pico里面有什么工具，工具调用怎么做的，怎么加新工具

===== PAGE 153/167 =====

delegate，可以把一个小调查任务交给只读的子 agent 去做。
工具调用这条链路，我做的是模型提申请，runtime 做安检，然后才执行。
模型在 prompt 里先看到工具清单、参数格式和调用示例，返回的是结构化的工具调用。
拦连续重复调用，高风险工具再走审批，最后才真正执行。执行完以后，结果会写回 history、
trace、report；如果涉及读写文件，还会顺手更新 memory，所以后面的上下文、恢复、审计都能接
护，就是如果模型连续两次发起完全一样的工具调用，runtime 会直接拦掉，避免它在没有新信息的
这样接完以后，现有的审批、审计、重复调用拦截、路径边界这些护栏会自动接上。如果这个工具会
影响工作区或者会产生对后续推理有价值的结果，我再补 memory 更新和测试。

===== PAGE 155/167 =====

作区状态和 verifier 能不能对上。 code agent 评测和普通问答不一样，很多时候真正要测的是它有
数之后能不能恢复，重复读同一个文件会不会被识别成坏循环。这一层更像是在测 runtime 的基本执
第二类是上下文相关的压力场景。这个不是靠真实任务自然出现，而是我会主动注入更长的 
history、更多的 note、不同长度的 request，看 prompt 变厚以后，上下文裁剪会不会把当前请求裁
第三类是记忆依赖任务。这类任务我会故意拆成两段，前一段先读文件、确认事实，后一段再问它刚
才那个事实是什么或者基于刚才那个约束继续做。这样就能很直接地测出，结构化记忆到底有没有真
checkpoint 会不会被错当成可用状态。主要测试系统有没有把旧状态信错，因为对 code agent 来
后是正常结束、步数打满，还是重试过多停下来的。这层回答的是Agent最后停在哪。

===== PAGE 156/167 =====

verifier 通过率；看上下文治理，我就看 prompt 长度、压缩率、当前请求有没有被裁坏；看记忆收
区漂移识别率、以及有没有误接受旧状态。这样拆开的好处是，一旦某次运行不对，我可以很清晰的
知道哪里出了问题，到底是任务固定格式本身有问题，还是工具调用被拦了，还是上下文裁剪出了问

===== PAGE 157/167 =====

经常会临时插入一个小问题。如果这类问题直接并入主执行链，就会污染任务状态、打断推理节奏，
甚至破坏工具调用闭环。
所以它比较好的设计方式，是把它做成一个和主任务并行、但共享上下文视图的轻量子会话。
这个子会话只继承必要上下文，不继承执行权；也就是说它可以读到当前任务的语义背景，但不能接
破坏，用户获得低延迟答复，以及上下文复用带来的成本控制
pico 当前更像一个单线程、同步推进的 agent runtime：CLI 层负责少量本地命令，核心 runtime 负
是‘受控旁路’。也就是在系统里新增一种 side-question 会话类型：它共享父会话的上下文快照和记

===== PAGE 158/167 =====

15. 整个项目中ai coding的占比，会用code agent辅助开发做些什么
16. 你觉得一个好的code agent应该是什么样的，未来code agent的演进形势

===== PAGE 159/167 =====

1. 拉开差距的，往往不是模型，而是 harness。同一个模型在不同 code agent 里表现差很多，通
2. 记忆要外部化，单靠上下文独木难支。比如Claude Code 明确把 ﻿CLAUDE.md﻿ 和 auto 
memory 作为跨 session 的项目记忆，未来高质量 memory 更像 repo 里的工程规范、约定和
3. 形态最终会走向 agent team。未来工程师越来越像 supervisor，agent 更像可委派的执行单

===== PAGE 160/167 =====

比如我前段时间为了系统研究大模型后训练流程，就专门搭了一个学习仓库，把论文、博客、课程、
2. 多看claude code怎么用的，从产品的角度去思考怎么改进自己的code agent：

===== PAGE 161/167 =====

第四是上下文和资源引用。Skill 要告诉模型优先使用哪些信息，比如项目 README、接口 schema、现
是最有用的，至于什么场景触发边界，那些都很随缘，很多模型都会忽略一部分上下文，但是我发现它
总结：好的 Skill 是一个面向具体问题的、可复用的、边界清晰的、能被评估和持续优化的 Agent 能

===== PAGE 162/167 =====

我理解的优秀 Prompt，是能让模型在明确目标、充分上下文、清晰约束和可验证标准下稳定完成任务。
第二是上下文充分。模型需要知道相关文件、错误日志、接口定义、已有实现、测试方式、业务约束。
目标、上下文、约束和完成标准。这样可以减少模型猜测，让产出更容易 review。
不要直接改代码。对于复杂任务，我会让模型先分析和规划，再实现。Cursor 的 agent 实践也强调先规
划、再编码，并让 agent 先研究代码库、提出问题、生成包含文件路径和代码引用的计划。

===== PAGE 163/167 =====

试命令和外部工具调用说明。

===== PAGE 164/167 =====

Agent 能力。
的上下文和约束不同。
第四步，让 AI 自己跑验证。包括单测、lint、类型检查、构建、相关 e2e 测试。Cursor 的 agent 实践
也强调要给 agent 可验证目标，例如类型系统、lint 和测试，因为 agent 需要明确知道什么结果算正
第一，我会给它足够上下文，包括 PR 需求背景、diff、相关测试、接口定义、项目规范和 CI 结果。因

===== PAGE 167/167 =====

第六，总结监控指标，比如错误率、P95、连接池、慢查询、MQ 堆积等，帮助定位可疑模块。
但 AI 给出的根因只能作为候选假设，最终必须通过日志、监控、trace、代码 diff 和复现验证。生产修